==NOW==
- parse args
- cleanup
- Dual boot


==NEXT==
- redo spring
- print banners
  - maybe spinners?
- continue cleaning up readme/shorten it
- should have a readme gif - very quick
- add comments
- Warning: apt-key is deprecated. Manage keyring files in trusted.gpg.d instead (see apt-key(8)).
  - sudo curl -fsSL https://example.com/key.gpg | sudo gpg --dearmor -o /etc/apt/trusted.gpg.d/key.gpg



redo eclipses

redo spring stuff

use markdown table

use try catch

timeouts